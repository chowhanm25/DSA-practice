# Design HashSet

This is a solution for the Design HashSet problem.

## Description
- Implements a hash set with basic operations: add, remove, and contains.

## Usage
- `add(key)`: Adds a key to the hash set.
- `remove(key)`: Removes the key from the hash set.
- `contains(key)`: Returns true if the key exists, false otherwise.

